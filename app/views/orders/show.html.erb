<table class='table'>
  <thead>
    <tr>
      <% ["Data", "Vacina", "Clinica", "Quantidade", "Preço"].each do |header| %>
        <th> <%= header %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td>
          <%= @order.created_at.strftime("%d/%m/%Y")  %>
        </td>
        <td>
          <%= @order.vaccine.vaccine_type.name  %>
        </td>
        <td>
          <%= @order.vaccine.clinic.name  %>
        </td>
        <td>
          <%= @order.quantity %>
        </td>
        <td>
          <%= @order.string_price  %>
        </td>
      </tr>
  </tbody>
</table>

<div class="review">
  <% if @order.review && @order.review.id %>
    <h2>Sua avaliação:</h2>
    <div class='container' style="width: 600px;">
      <table class="table">
        <thead>
          <tr>
            <th>
              Classificação
            </th>
            <th>
              Descrição
            </th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <% @order.review.stars.times do  %>
               <i class="fas fa-star"></i>
              <% end %>
              <% (5-@order.review.stars).times do  %>
               <i class="far fa-star"></i>
              <% end %>
            </td>
            <td>
              <%= @order.review.description %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  <% else %>
    <h2>Deixe sua avaliação:</h2>
    <%= render "reviews/new" %>
  <% end %>
</div>

<%= link_to "Voltar", orders_path, class: "btn btn-primary" %>
